# 北京公交问数产品泛化验证方案知识库文档

**文档版本：** V1.0
**创建日期：** 2026-07-15
**文档用途：** 团队评审与决策依据、泛化验证任务背景与可行性评估

---

## 第一部分：需求描述

### 1.1 项目背景与现状

北京公交问答问数产品已完成第一阶段建设，采用 **NL2API + NL2SQL 兜底** 的混合架构，实现了基于公交数据湖的自然语言查询能力。

**已建成能力概要：**

- **用户体系：** 集团用户、集团专业用户、分公司用户三级权限体系
- **问数流程：** 数据接入 → 样例配置 → 关键词配置 → 技能管理 → 创建助理
- **数据库支持：** DORIS / MYSQL / ORACLE
- **已覆盖业务域：** 11 个业务域，涵盖线路、站点、车辆、场站、能源站、车辆到离站、充电/加油/加气/加氢交易、刷卡刷码等核心运营场景
- **调优体系：** 模型调优、应用配置及知识调优、页面展示和交互调优三个维度

**现阶段核心成果：** 11 个业务域的问数场景已完成样例配置、关键词配置和 SQL 模版沉淀，形成了可复用的 NL2API 工程化经验。

---

### 1.2 泛化需求分析

#### 1.2.1 泛化目标

下阶段需要将现有 NL2API 中的 SQL 模版能力，泛化至**安全服务（安服）和经营指标**两大新场景。

| 维度 | 已有场景（一期） | 泛化目标（二期） |
|------|--------------|--------------|
| 业务域数量 | 11 个 | +6 大业务域 |
| 核心概念数 | — | 50 个 BusinessConcept |
| 知识三元组 | — | 748 个 |
| 数据库方言 | DORIS/MYSQL/ORACLE | Apache Doris（统一方言） |

#### 1.2.2 本体 Schema 结构说明

安服和经营场景的知识本体采用三层 Class 层次结构：

```
BusinessConcept（业务概念）
├── Entity（静态主数据）      → 车辆/人员/线路/场站等维表
├── Event（实时/告警事件）    → ADAS/DMS/CAN/进出站/超速等
└── Report（统计报表）        → 线路运营指标/客流/准点/里程等
```

**六大业务域分布：**

| 域代码 | 中文名 | 概念数 | 定位描述 |
|--------|--------|--------|----------|
| O | 运营核心 | 19 | 线路运营指标、客流、运力、准点、里程等 |
| MD | 主数据 | 10 | 车辆/人员/线路/场站等静态维表 |
| S | 安全告警 | 18 | ADAS/DMS/CAN/进出站/超速等实时告警 |
| D | 驾驶员管理 | 18 | 驾驶时长、健康、资质、画像等 |
| SC | 稽查客诉 | 2 | 综合稽查与市民投诉 |
| P | 乘客客流 | 3 | 时段/老年客流 |

#### 1.2.3 泛化的核心挑战

**挑战一：样例数据冷启动**
50 个 BusinessConcept 均需配置 query-SQL 样例对，人工编写成本高（每个概念预估需要 3-10 条样例）。

**挑战二：SQL 模版参数化复杂度**
新场景的 SQL 模版参数化占位符涉及多维度：`{date}`、`{date_start}/{date_end}`、`{n_min}`、`{line}`、`{org}`、`{gh}`、`{zbh}`、`{rank_n}`，手工编写容易出错且难以保证一致性。

**挑战三：意图路由准确性验证**
两级路由策略（优先规则 AND 逻辑 + TTL 兜底 OR 逻辑）需要大量测试用例覆盖，通过率基线要求 ≥ 46/46（100%）。

**挑战四：质量回归成本**
每次调优迭代均需人工验证 SQL 正确性，缺乏自动化回归手段，影响交付节奏。

---

### 1.3 智能体定位与价值

#### 1.3.1 智能体定位

「**query + sql 样例生成和自动化测试智能体**」是一个面向问数场景泛化调优阶段的效率工具，核心能力为：

1. **样例自动生成：** 基于业务本体 Schema（BusinessConcept 定义、SQL 模板、关键词）自动生成 query-SQL 样例对，覆盖 50 个概念的多样化问法
2. **自动化测试：** 对生成的样例执行端到端管道测试，按照既定评分标准输出质量报告
3. **人工审核辅助：** 提供结构化的人工审核界面（原型地址已可访问），帮助效果 Owner 高效完成样例筛选与质量确认

#### 1.3.2 智能体价值

**效率价值：**

| 环节 | 无智能体（人工） | 有智能体辅助 | 预计提效 |
|------|----------------|-------------|---------|
| 样例编写（50 概念 × 5 条均值） | ~250 人时 | ~25 人时（审核） | ~90% |
| SQL 语法验证 | 逐条执行，约 2 分/条 | 批量自动化，秒级 | ~95% |
| 回归测试（每次迭代） | 2-3 天 | < 1 小时 | ~85% |
| 意图路由覆盖率统计 | 手动整理 | 自动报告输出 | ~100% |

**质量价值：**
- 自动化评分覆盖：意图路由（路由准确率）、SQL 生成（表名/过滤/聚合/字段四维度）、端到端管道（实体识别/时态解析/聚合语义/参数替换）
- 建立持续集成质量基线，支持调优回归防劣化

**知识沉淀价值：**
- 自动生成的优质样例可直接导入问数产品作为配置资产
- 形成安服和经营场景的标准测试集，供后续其他场景复用

---

### 1.4 验证需求与目标

#### 1.4.1 验证策略

验证流程采用「人工审核 + 系统验证」双重保障：

```
智能体生成 query+SQL 样例
        ↓
人工审核（效果 Owner 把关质量和业务准确性）
        ↓
审核通过的样例 → 调用大模型问数产品后端
        ↓
后端调用公交数据湖 SQL 查询统一接口执行验证
        ↓
输出验证报告（执行成功率 + 结果合理性）
```

#### 1.4.2 验证目标与通过标准

| 验证维度 | 评分方式 | 通过基线 |
|----------|---------|---------|
| 意图路由准确率 | 完全正确 1.0 / 域正确 0.3 / 完全错误 0.0 | ≥ 46/46（100%） |
| SQL 生成质量 | 表名正确性 30 + 过滤条件 30 + 聚合逻辑 20 + 字段选择 20 | ≥ 85 分 |
| 端到端管道 | 实体识别 + 时态解析 + 聚合语义 + 参数替换 | 30/30 全通过 |
| 数据湖执行成功率 | SQL 在数据湖实际执行无报错 | ≥ 90%（目标） |

#### 1.4.3 验证阶段划分

- **阶段一（冒烟验证）：** 选取各业务域代表性概念（6域 × 2概念 = ~12概念），验证端到端链路可用性
- **阶段二（全量验证）：** 覆盖全部 50 个 BusinessConcept，完成意图路由和 SQL 生成双维度评分
- **阶段三（回归验证）：** 每次调优迭代后自动触发回归，保障质量基线不劣化

---

## 第二部分：集成架构

### 2.1 整体架构图（文本描述）

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户交互层                                    │
│                                                                     │
│   ┌─────────────────────┐        ┌──────────────────────────────┐   │
│   │   问数产品前端        │        │  智能体审核界面（原型）         │   │
│   │  （集团/分公司用户）  │        │  aiqueryexample.gzbh-...     │   │
│   └──────────┬──────────┘        └──────────────┬───────────────┘   │
└──────────────┼───────────────────────────────────┼─────────────────┘
               │                                   │
┌──────────────▼───────────────────────────────────▼─────────────────┐
│                        智能体核心层                                   │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │              query + sql 样例生成和自动化测试智能体             │    │
│  │                                                            │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │    │
│  │  │  样例生成模块  │  │  自动化测试   │  │  人工审核辅助    │  │    │
│  │  │  (NL2Query)  │  │  评分模块     │  │  管理模块        │  │    │
│  │  └──────┬───────┘  └──────┬───────┘  └────────┬────────┘  │    │
│  │         │                 │                    │           │    │
│  │  ┌──────▼─────────────────▼────────────────────▼────────┐  │    │
│  │  │              Schema 解析 & 意图路由引擎                 │  │    │
│  │  │   (BusinessConcept / triggerKeywords / queryTemplate) │  │    │
│  │  └───────────────────────────────────────────────────────┘  │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────┬───────────────────────────────────┘
                                  │
┌─────────────────────────────────▼───────────────────────────────────┐
│                     大模型问数产品后端                                 │
│                                                                     │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────────┐   │
│  │  NL2API 引擎     │  │  NL2SQL 兜底引擎 │  │  技能/助理管理    │   │
│  │  (SQL模版匹配)   │  │  (大模型直接生成) │  │  (样例/关键词配置)│   │
│  └────────┬────────┘  └────────┬────────┘  └──────────────────┘   │
│           └──────────┬─────────┘                                   │
│                      │                                              │
│  ┌───────────────────▼──────────────────────────────────────────┐  │
│  │              SQL 查询统一接口（数据湖访问网关）                   │  │
│  └───────────────────┬──────────────────────────────────────────┘  │
└──────────────────────┼──────────────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────────────┐
│                    公交数据湖（Apache Doris）                          │
│                                                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐  │
│  │ 运营核心O │ │ 主数据MD │ │ 安全告警S │ │驾驶员管理D│ │稽查客诉SC│  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └─────────┘  │
│                             ┌──────────┐                            │
│                             │ 乘客客流P│                            │
│                             └──────────┘                            │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 2.2 智能体核心模块设计

#### 2.2.1 样例生成模块

**输入：** BusinessConcept Schema（primaryTable、keyMetrics、queryTemplate、triggerKeywords、businessRule）

**处理逻辑：**
1. 解析 queryTemplate 中的参数化占位符（`{date}`、`{line}` 等）
2. 基于 triggerKeywords 生成多样化自然语言 query（覆盖同义词替换、时间跨度变化、聚合维度变化）
3. 将生成的 query 与对应 SQL 模版（填充合理参数）配对
4. 输出结构化样例对：`{ query: string, sql: string, concept_id: string, domain: string, params: object }`

**生成策略：**
- **模版扩展：** 基于每个概念的 queryTemplate 生成 3-10 条变体
- **参数枚举：** 对 `{n_min}`、`{rank_n}` 等数值参数自动枚举常见值
- **时间表达多样化：** 对 `{date}`、`{date_start}/{date_end}` 生成「昨天」「上周」「本月」「近 N 天」等多种表达

#### 2.2.2 自动化测试评分模块

该模块按三个维度独立评分，输出结构化评分报告：

**维度一：意图路由评分**

```
输入: query 文本
流程: 
  1. 两级路由引擎执行（优先规则 AND → TTL 兜底 OR）
  2. 匹配结果与预期 concept_id 对比
评分规则:
  - 完全正确（concept 匹配）: 1.0
  - 域正确（domain 匹配，concept 错误）: 0.3
  - 完全错误: 0.0
通过基线: 所有样例总分 ≥ 46/46 等价加权分
```

**维度二：SQL 生成质量评分**

```
输入: 路由后生成的 SQL
评分维度（满分100分）:
  - 表名正确性: 30分（主表 + altTables 命中）
  - 过滤条件: 30分（WHERE 子句中分区字段、时间字段、业务过滤字段）
  - 聚合逻辑: 20分（GROUP BY / 聚合函数与 keyMetrics 对应）
  - 字段选择: 20分（SELECT 字段与业务问题相关性）
通过基线: 平均分 ≥ 85分
```

**维度三：端到端管道评分**

```
输入: query → 路由 → SQL生成 → 参数替换 全链路输出
评分项（各项二元通过/失败）:
  - 实体识别: {line}/{org}/{gh}/{zbh} 等实体正确提取
  - 时态解析: {date}/{date_start}/{date_end} 时间范围正确解析
  - 聚合语义: 聚合方式（SUM/AVG/COUNT/TOP N）与 query 语义一致
  - 参数替换: 所有占位符均被有效值替换，无残留占位符
通过基线: 30/30 全部通过
```

#### 2.2.3 人工审核辅助管理模块

**功能设计：**
- **样例列表视图：** 展示生成的 query-SQL 对，标注自动化评分结果
- **批量审核操作：** 支持「通过 / 驳回 / 修改后通过」三种操作
- **驳回原因标注：** 内置驳回原因枚举（SQL错误 / 业务理解偏差 / 语义不自然 / 参数异常）
- **审核进度看板：** 按业务域展示审核进度（已审 / 待审 / 驳回率统计）
- **导出功能：** 审核通过的样例支持导出为问数产品后端所需的标准格式（JSON/CSV）

---

### 2.3 数据流与验证链路

#### 2.3.1 样例生成链路

```
本体 Schema（JSON）
    │
    ▼
Schema 解析器
    │ 提取: primaryTable, queryTemplate, triggerKeywords, keyMetrics
    ▼
样例生成引擎（LLM 辅助 + 规则扩展）
    │ 输出: query-SQL 样例对（含元数据）
    ▼
自动化评分引擎
    │ 输出: 三维度评分报告
    ▼
人工审核队列
    │ 操作: 通过 / 驳回 / 修改
    ▼
审核通过样例库
    │
    ├──→ 导出为问数产品样例配置格式（直接复用）
    └──→ 进入验证链路
```

#### 2.3.2 验证链路（SQL 执行验证）

```
审核通过的 query-SQL 样例
    │
    ▼
调用大模型问数产品后端 API
    │ 参数: query 文本、所属 skill/助理 ID
    ▼
大模型问数产品后端
    │ 执行: NL2API 意图路由 → SQL 模版填充
    ▼
SQL 查询统一接口（数据湖访问网关）
    │ 执行: Apache Doris 实际查询
    ▼
返回查询结果 / 错误信息
    │
    ▼
验证结果收集器
    │ 记录: 执行成功率、错误分类、结果数据样本
    ▼
验证报告生成
    │ 输出: 按业务域汇总、问题 SQL 列表、建议修改意见
    ▼
反馈给效果 Owner（DPM 范俊臣）
```

#### 2.3.3 调优迭代闭环

```
验证报告中的问题样例
    ↓
效果 Owner 分析根因（路由错误 / SQL 错误 / 参数解析错误）
    ↓
三种修复路径：
    ├── 路由问题 → 调整 triggerKeywords / 优先规则
    ├── SQL 问题 → 修正 queryTemplate / 样例 SQL
    └── 配置问题 → 调整关键词配置 / 字段描述
        ↓
重新触发自动化测试（回归验证）
        ↓
质量基线对比（与上次迭代结果对比，防止劣化）
```

---

### 2.4 与大模型问数产品后端的集成方式

#### 2.4.1 集成接口设计

智能体与大模型问数产品后端的集成通过以下两类接口实现：

**接口一：SQL 执行验证接口**

```
接口功能: 将审核通过的 query 提交到后端执行，获取 SQL 和查询结果
调用方: 自动化测试智能体（验证链路）
被调用方: 大模型问数产品后端 → 公交数据湖 SQL 查询统一接口
请求参数: 
  - query: 自然语言问题
  - assistant_id / skill_id: 目标技能/助理标识
  - domain: 业务域标识（用于结果校验）
返回内容:
  - generated_sql: 后端生成的 SQL
  - execution_result: 查询结果（行数、数据样本）
  - execution_status: success / error
  - error_message: 错误详情（如有）
```

**接口二：样例批量导入接口**

```
接口功能: 将审核通过的 query-SQL 样例批量导入问数产品的样例配置
调用方: 智能体审核模块（导出环节）
被调用方: 大模型问数产品后端（样例配置管理模块）
请求参数:
  - samples: [ { query, sql, table, domain, concept_id } ]
  - skill_id: 目标技能 ID
  - import_mode: append / overwrite
返回内容:
  - imported_count: 成功导入数量
  - failed_items: 导入失败条目及原因
```

#### 2.4.2 集成改造需求（产品侧）

由 RD 宋世鹏、RD 杨发英 负责以下后端改造：

| 改造项 | 描述 | 优先级 |
|--------|------|--------|
| SQL 执行验证接口开放 | 暴露公交数据湖 SQL 查询统一接口，供智能体调用 | P0 |
| 执行结果结构化返回 | 在现有接口基础上增加 generated_sql、execution_status 字段 | P0 |
| 样例批量导入接口 | 支持外部系统批量写入样例配置 | P1 |
| 接口鉴权 | 基于 API Key 或 JWT 的调用鉴权，限定调用来源 | P0 |

---

### 2.5 安全保障设计

#### 2.5.1 数据安全

| 风险点 | 保障措施 |
|--------|---------|
| 生成的 SQL 可能产生全表扫描 | 所有 SQL 执行前强制校验 `WHERE` 子句存在性；禁止无条件 `DELETE/UPDATE/DROP` |
| 验证接口暴露数据查询能力 | 接口仅开放 `SELECT` 权限，在数据湖网关层做 DDL/DML 拦截 |
| 样例数据包含业务敏感字段 | 样例生成时对敏感字段（如驾驶员工号 `{gh}`、车牌号 `{zbh}`）使用脱敏占位符，不存储真实值 |
| 测试结果数据留存 | 验证结果仅保留统计摘要（行数、成功率），不保留完整数据行 |

#### 2.5.2 链路安全

| 风险点 | 保障措施 |
|--------|---------|
| 智能体非授权调用后端接口 | 接口层实施 API Key 鉴权 + 白名单 IP 限制 |
| 恶意 SQL 注入 | 后端 SQL 执行层强制参数化处理，禁止拼接执行 |
| 样例导入越权 | 样例导入接口绑定具体 skill_id，权限与用户角色体系对齐 |
| 并发压测风险 | 自动化测试执行并发度设上限（建议 ≤ 10 并发），避免冲击数据湖 |

#### 2.5.3 审核流程安全

- 样例通过人工审核后才允许进入验证链路，**禁止未审核样例直接触发 SQL 执行**
- 效果 Owner（DPM 范俊臣）具有最终质量把关权，审核记录完整留存
- 导入正式产品配置的操作需二次确认，并记录操作日志

---

## 第三部分：协同分工表

### 3.1 角色职责矩阵

| 角色 | 人员 | 职责描述 | 关键交付物 | 依赖关系 |
|------|------|---------|-----------|---------|
| **智能体技术负责人** | DTA 王坤 | 负责智能体整体搭建，包括样例生成模块、自动化测试评分模块、人工审核界面的工程实现；负责数据端（本体 Schema）集成和整体功能流程端到端验证 | ① 智能体原型可用版本<br>② Schema 解析与生成功能<br>③ 自动化测试评分模块<br>④ 整体验证流程测试报告 | 依赖：RD 提供后端 API 接口文档；依赖 DPM 提供 Schema 和业务规则输入 |
| **效果质量 Owner** | DPM 范俊臣 | 作为效果 Owner，负责开发和验证问数产品 Skill（技能配置、关键词、样例）；对生成样例做业务层人工审核；保障验证链路的数据正确性和链路安全性；对调优效果负最终责任 | ① 50 个 BusinessConcept 的 Schema 整理<br>② Skill 配置文档<br>③ 人工审核完成报告<br>④ 调优迭代记录<br>⑤ 链路安全评估报告 | 依赖：王坤提供智能体生成样例和评分报告；依赖 RD 接口稳定可用 |
| **后端研发（接口改造）** | RD 宋世鹏 | 负责大模型问数产品后端的产品侧改造，重点负责 SQL 执行验证接口的开放与结构化返回改造；负责接口鉴权机制实现 | ① SQL 执行验证接口文档<br>② 接口上线并通过联调测试<br>③ 鉴权方案实现 | 依赖：范俊臣提供 Skill 配置和 assistant_id；需与王坤完成接口联调 |
| **后端研发（样例导入）** | RD 杨发英 | 负责样例批量导入接口的开发；支持智能体审核模块与问数产品后端样例配置的对接；协助数据湖 SQL 执行权限管控改造 | ① 样例批量导入接口文档<br>② 接口上线并通过联调测试<br>③ 数据湖 SQL 执行权限管控改造完成 | 依赖：范俊臣提供样例格式规范；需与王坤完成导入联调 |

---

### 3.2 关键路径与里程碑

| 里程碑 | 目标描述 | 负责人 | 依赖前置 |
|--------|----------|--------|---------|
| M1：Schema 整理与接口设计 | 50 个 BusinessConcept 的 Schema 整理完成；SQL 执行验证接口和样例导入接口设计文档完成 | DPM 范俊臣 + RD 宋世鹏 | 无 |
| M2：智能体原型可用 | 样例生成模块、自动化评分模块、人工审核界面开发完成，原型可访问 | DTA 王坤 | M1（Schema 输入） |
| M3：后端接口开发完成 | SQL 执行验证接口、样例批量导入接口开发完成并提测 | RD 宋世鹏 + RD 杨发英 | M1（接口设计） |
| M4：端到端联调完成 | 智能体→后端接口→数据湖 SQL 执行全链路联调通过 | DTA 王坤 + RD 全体 | M2 + M3 |
| M5：冒烟验证完成 | 选取各业务域代表性概念（6域 × 2概念 ≈ 12概念）完成端到端验证 | DPM 范俊臣 | M4 |
| M6：全量验证完成 | 覆盖全部 50 个 BusinessConcept，意图路由和 SQL 生成双维度评分达标 | DPM 范俊臣 | M5 |
| M7：调优闭环与回归 | 完成首轮调优迭代，回归验证通过，质量基线确立 | DPM 范俊臣 | M6 |

关键路径：M1 → M2/M3（并行）→ M4 → M5 → M6 → M7

---

### 3.3 协同协作机制

**1. 日常同步**
- 每日站会（15 分钟）：各角色同步进展、阻塞点
- 每周周会：复盘完成情况，确认下周计划，同步风险项
- 需求变更通过文档提单，变更影响评估后统一排期

**2. 接口协作规范**
- 后端接口文档须在开发启动前完成并同步智能体侧
- 智能体在接口未就绪前可基于 Mock 数据并行开发，接口就绪后切换真实联调
- 联调期间发现的接口问题须在当日内定责并给出修复时间

**3. 审核与安全协作**
- 样例通过人工审核后才允许进入验证链路，禁止未审核样例直接触发 SQL 执行
- 效果 Owner（DPM 范俊臣）具有最终质量把关权，审核记录完整留存
- 导入正式产品配置的操作需二次确认，并记录操作日志

---

### 3.4 风险与应对措施

| 风险 | 等级 | 影响范围 | 应对措施 | 责任人 |
|------|------|---------|---------|--------|
| 数据湖 SQL 执行权限管控涉及底层改造，技术方案存在不确定性 | 高 | 后端研发、联调进度 | 提前进行技术预研；若复杂度超预期，及时沟通缩小首期范围，分阶段交付 | RD 宋世鹏/杨发英 |
| 智能体生成的样例质量不达标，人工审核驳回率高 | 中 | 验证进度、调优成本 | 迭代优化生成策略（prompt 调优、规则增强）；建立驳回原因分类统计，针对性改进 | DTA 王坤 + DPM 范俊臣 |
| 意图路由准确率无法达到 46/46 基线 | 中 | 全量验证里程碑 | 分析路由失败 case，调整 triggerKeywords 和优先规则；必要时增加样例辅助路由 | DPM 范俊臣 |
| 后端接口变更或不稳定影响联调 | 中 | 智能体验证链路 | 接口层增加降级策略；与 RD 确认接口稳定性承诺 | DTA 王坤 + RD |
| 并发测试冲击数据湖性能 | 低 | 数据湖稳定性 | 自动化测试执行并发度设上限（建议 ≤ 10 并发），避开业务高峰期执行 | DPM 范俊臣 |

---

## 附录：参考文档

| 文档 | 说明 |
|------|------|
| DuMate 切换 Claude 模型的经验总结 | cc 技能参考文档，OneAPI 平台接入 Claude 的配置方法 |
| 智能助理运营手册（北京公交） | 问数产品操作手册，含数据接入、样例配置、关键词配置、调优指南 |
| 北京公交问数功能范围 | 11 个已建问数场景的字段与问法清单 |
| 北京公交经营和安服指标看板本体 Schema 与测试问题 | 50 个 BusinessConcept 的本体定义、SQL 模板、测试问题集和评分标准 |

---

*文档版本: V1.0*
*创建日期: 2026-07-15*
*产出工具: Claude Sonnet 4.6 via OneAPI（cc 技能）*
